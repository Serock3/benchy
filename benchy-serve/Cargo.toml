[package]
name = "benchy-serve"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"

[dependencies]
benchy-lib = { path = "../benchy-lib" }
benchy-store = { path = "../benchy-store" }
log = "0.4"
rocket = { version = "0.5", features = ["json"] }

[dev-dependencies]
tempfile = "3"
